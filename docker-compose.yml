version: "3.8"
services:
  databento:
    image: mcp/databento:latest
    container_name: databento-container
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8008:8008"
